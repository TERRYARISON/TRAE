#

帮我做一个 iOS App。

【目标】

一个单用户专属的个人 AI Copilot：1 个 AI 助理 + 3 个内容区（知识库/笔记/代码库），助理对所有内容能读、能建、能改、能整理；能处理任意规模的巨型任务（读几千万字、写百万字级别）。整体体验对标腾讯 ima Copilot 的「我的 Copilot」模式，但纯粹是我个人纯享版。

【用户】

给我自己用（单用户、无账号系统）。解决三个问题：

① 知识、笔记、代码散落各处，没有统一的 AI 帮我管&#x20;

② 普通 AI 对话处理不了超出上下文的超大任务&#x20;

③ 解决写一切创作阅读编辑文字和代码的需求。

【核心功能】

1. AI 助理（对话）
   * 全局唯一助理：任何入口唤起的都是同一个它，共用同一套记忆和上下文
   * 流式回复；回答知识库内的问题必须带来源角标
   * 联网搜索（可开关，结果标来源）
   * 深度研究模式：多轮递进搜索 → 自检缺口补搜 → 输出结构化报告（结论/论据/来源/缺口），报告自动存为笔记
   * 可派活：让助理新建/修改/整理/编辑笔记、知识库文件、代码项目
2. 知识库：文件夹/标签/搜索；导入各类常见文档；助理可检索、归类、重命名、入库
3. 笔记：即可编辑区笔记本 + Markdown；对话里说「帮我记一下」直接建笔记；AI 可帮写/润色/追加
4. 代码库：代码项目/片段的个人馆；助理可读、可改、可整理
5. 多模型接入：任意 OpenAI 兼容 API 都能接（自配接口地址+密钥，密钥加密存储）；多套配置保存、一键切换；每个对话独立选模型；推理强度五档 Off / Low / Medium / High / Max（不支持的模型自动隐藏该选项）；需要"派活干活"的模型必须支持工具调用，不支持的在选择器里标注「仅对话」
6. 技能：提示词工作流包（名称/描述/触发词/流程）；创建/编辑/启停/删除/导入导出；先内置 4 个装机技能:长文阅读、长文输出、创建技能、ponytail。
7. 记忆：跨会话记住我；
8. 四张卡片（助理人设/我的档案/长期记忆/经验技巧）；可视、可编辑、可删除、有总开关；对话里能自然语言管理（「以后叫我XX」「删掉关于X的记忆」）
9. 安全底线：删除/覆盖/批量移动必须二次确认；
10. **OCR 使用 iOS 原生**&#x20;
11. **导入导出文件格式和主流 llm app 一样即可。**
12. **本地小模型接口留一个**

【数据】

全部存在手机本地，不上传云盘、不做云同步：

* 对话记录（含任务历史与产物）
* 知识库文件及其内容索引
* 笔记（含笔记本归属）
* 代码项目
* 记忆四卡片内容
* 技能
* 模型配置（密钥加密存储）
* 巨型任务的执行状态与产物文件

数据主权归我：随时可导出、可备份恢复。

【iOS 描述 / UI 描述】

* iPhone 原生 App，目标机型 iPhone 16 Pro Max（系统 iOS 18 以上），界面简体中文
* 底部 4 个 Tab：💬 对话（默认首页）/ 📚 知识库 / 📝 笔记 / 💻 代码库——设置不占 Tab
* 对话页双抽屉：左上 ☰ = 历史会话；右上助理头像 = 「助理」抽屉（人设 → 记忆 → 技能 → 模型 → 偏好 → 备份，全部设置都在这）
* 对话输入区从左到右：📎附件/📷拍照 ｜ 🌐联网、🧠深度研究（开关，选中高亮）｜ 模型选择器（常驻显示当前模型名，点开含推理档位）｜ 发送键；输入 @ 弹出知识库引用选择器
* 三个内容区右上角统一放「✨ 让助理处理」
* 巨型任务运行时：对话内出任务进度卡（阶段+百分比+当前动作），灵动岛/锁屏实时活动同步显示；iPhone 的 Action 按钮默认 = 暂停/继续大任务
* 普通长对话：聊得太长时自动把早期内容压缩成摘要气泡（可点开看压缩了什么），永远不会聊爆
* 视觉：**OCR 使用 iOS 原生**&#x20;

【偏好】

最重要的能力：处理各种巨型任务。任何类型的超大任务——读完几千万字、写出百万字、全库整理、批量翻译、深度调研、知识库……都要能稳定完成。不限于任何预置场景：我提什么任务，它就能拆解什么任务。

【特点补充】巨型任务的执行逻辑（行为要求）

1. 先计划后执行：任何巨型任务先给我看拆解计划（分几步、每步干什么），我确认才开跑
2. 子任务并行：能并行的子任务同时跑（像多个子代理分工协作），总进度按子任务聚合显示
3. 无限输入：材料再多也能「读完」——分批消化成笔记再汇总成全局理解，绝不因为文件太大而失败或遗漏
4. 无限输出：长文按大纲逐段推进，始终记得前面的设定与伏笔（人物/时间线/口径不乱）
5. 断点续跑：随时暂停/取消；杀掉 App 重开，从断点无损继续
6. 自检审计：阶段性自查（矛盾/遗漏/缺口），发现问题自动补救或汇报给我
7. 过程透明：每一步在做什么、产出了什么，随时点开可查
8. 稳定不劣化：无论任务多大、跑多久，单次处理量保持稳定，速度和质量不随任务变长而下降

（验收示例：导入 1000 万字语料后提问能答且带出处；100 万字写作任务中途杀 App 重启后无损续写；一个从未预置的新任务类型也能自动拆解、确认后完整跑完交付）

【边界】无需

账号系统、多端、任何社交/分享/社区/广场；语音输入、协作与多人权限。
